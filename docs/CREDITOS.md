# Créditos y licencia

- **Repositorio**: `josueluz89/scraperstv-providers` — parte del proyecto MasterScrap.
- **Origen de los providers vendorizados**: varios de los `.js` de `providers/` proceden de
  **Nuvio** (proyecto de providers para streaming, GPL-3.0) y se han adaptado; los que tienen
  fuentes en `src/` son propios.
- **Runtime**: los providers se ejecutan en **QuickJS** dentro de la app (módulo del motor),
  imitando el entorno que da Nuvio.
- **Licencia**: GPL-3.0 (la que declara `package.json`). Si redistribuyes el repo, mantén este
  aviso y la licencia.
